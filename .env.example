# Environment Variables

# OpenLigaDB API (no key required for public endpoints)
OPENLIGADB_BASE_URL=https://api.openligadb.de

# Cache TTL in seconds (default 5 minutes)
CACHE_TTL_SECONDS=300

# Node Environment
NODE_ENV=production
